function large(str){
    var len = str.length, arr = new Array(len + 1);
    for(let i = 0; i <= len; i ++) arr[i] = new Array(len + 1);
    for(let i = 1; i <= len; i ++) arr[i][0] = 0;
    for(let i = 0; i <= len; i ++) arr[0][i] = 0;
    for(let i = 1, j; i <= len; i ++){
        for()
    }
}