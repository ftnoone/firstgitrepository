function longestCommonSequence(x, y){
    var m = x.length, n = y.length, i, j, len = n < m ? n : m, subarr = new Array(len);
    for(i = 0; i < len; i ++){
        subarr[i] = new Array(len);
    }
    
    for(i = 0; i < )
    return c;
}